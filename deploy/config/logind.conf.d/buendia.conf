# Keep the server running when the laptop lid is closed (safety net against an
# accidental close). Run the laptop LID-OPEN for cooling (plan §3.1 / §7 heat).
[Login]
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
HandleLidSwitchDocked=ignore
